# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
# Service role key (required for webhook processing - find in Supabase Dashboard > Settings > API)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL (REQUIRED for OAuth redirects in production)
# Development: leave empty (will default to http://localhost:3000)
# Production: MUST be set to your production domain (e.g., https://yourdomain.com)
NEXT_PUBLIC_APP_URL=

# Strava Integration
# Create an app at https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your-strava-client-id
STRAVA_CLIENT_SECRET=your-strava-client-secret
# Random string for webhook verification (you create this)
STRAVA_WEBHOOK_VERIFY_TOKEN=your-random-verify-token
